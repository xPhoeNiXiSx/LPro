#include<stdio.h>

#define sizeL 5
#define sizeC 3

int mat[sizeL][sizeC];
int i,j;

void main(int argc, char* argv [])
{
for(i=0;i<sizeL;i++)
{
	for(j=0;j<sizeL;j++)
	{
	printf("Valeur de la case (%d,%d):",i,j);
	scanf("%d",&mat[i][j]);
	}
}
