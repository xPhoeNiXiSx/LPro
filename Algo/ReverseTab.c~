//Demander des nombre a un utilisateur et faire en sorte que les valeur saffiche dans le sens inverse


#include<stdio.h>
#define size 6

int i,temp,ta[size];
void main[]
{
	for(i=0;i<size;i++)
	{
		printf("Donner un nombre/n");
		scanf("%d",&tab[i]);
	}// saisie des valeur dans un tableau dans le bon ordre

	for(i=0;i<size/2;i++)
	{
		temp=tab[i];
		tab[i] = tab[size-i-1];
		tab[size-i-1] = temp;
	}

	for(i=0;i<size;i++)
	{
		printf("%d",tab[i]);
		printf("/n");
	}
}



